# Check if strings are rotations of each other or not
# Basic

class Solution:
    def areRotations12(self,s1,s2):
        result=s1+s1
        if s2 in result:
           return 1
        return 0 
    
